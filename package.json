{
  "name": "netlify_functions_test",
  "version": "1.0.0",
  "description": "",
  "main": "api/hello.js",
  "license": "ISC",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "netlify-lambda serve resources/api --config ./webpack.functions.js --port 9001",
    "build": "netlify-lambda build resources/api"
  },
  "dependencies": {
    "axios": "^0.19.0",
    "dotenv-webpack": "^1.7.0",
    "netlify-lambda": "^1.5.0"
  }
}
